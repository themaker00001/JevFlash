.venv/
__pycache__/
*.pyc
.DS_Store

# Secrets -- never commit
.env

# Model checkpoints and tokenizer/config dumps are large binaries; keep only
# the small JSON summaries (train_log, summary, timing, target_audit) in git.
runs/**/*.safetensors
runs/**/tokenizer/
runs/**/backbone_config/
runs/**/*.jsonl

# Hugging Face download cache if it ever lands inside the project
.cache/

# ViZDoom writes its runtime config wherever the process's cwd happens to be
_vizdoom.ini
